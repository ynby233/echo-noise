# echo-noise 领域词汇

## 笔记生命周期

- **活动笔记**：尚未进入回收站、可以出现在正常内容入口中的笔记。
- **回收站笔记**：已经被删除但尚未永久删除的笔记。它不属于正常内容列表，只能在回收站上下文中处理。
- **永久删除**：从笔记生命周期中不可逆地移除笔记及其按规则处理的关联数据。

## 可见性与授权

- **正常可见性规则**：访客、登录用户、VoceChat 联系人和作者私密范围等现有受众规则的合称。
- **隐藏内容**：按照正常可见性规则，当前操作者本来无权读取的内容；它描述读取结果，不等同于回收站状态。
- **查看隐藏内容**：允许受托管理员读取隐藏内容的独立只读能力；它不授予点赞、评论、回复、置顶、编辑、改可见范围、删除或其他管理动作。1 号管理员创建的、对该受托管理员隐藏的内容不在此能力的可读取范围内。
- **受托管理员**：不是 1 号管理员、但被授予一个或多个管理员能力的管理员。
- **受保护内容**：受 1 号管理员所有权或系统保护规则约束的内容；受托管理员的管理动作不能越过该保护。
- **管理员内容互动限制**：任何管理员（包括 1 号管理员）都不能对自己按内容原始可见性规则隐藏的内容点赞、评论、回复或置顶；“查看隐藏内容”以及 1 号管理员的隐式查看权限都不解除这一限制。编辑和移入回收站是否可行由相应能力决定，但受托管理员不能越过 1 号管理员内容保护。

## 管理动作

- **回收站查看**：进入并读取回收站上下文的能力；恢复和永久删除必须以它为前提，但三项能力彼此独立配置。
- **管理入口能力**：决定是否可以进入某个管理上下文的能力，例如笔记管理或笔记回收站；它与“查看隐藏内容”分离，后者只扩大读取范围，不自动打开管理入口。
- **能力双层约束**：缺少某项能力时，前端不显示对应模块入口或操作控件；即使操作者绕过前端直接访问 URL、接口或构造请求，后端也必须拒绝并返回明确错误。前端隐藏不是安全边界，后端授权才是最终判定。
- **明确勾选**：管理员在界面中逐项或按当前页选择后形成的具体笔记 ID 集合；批量动作只处理这个集合，不隐含“全部筛选结果”。

## 附件与留言

- **附件可见性继承**：附件的可见范围继承其引用来源内容的可见范围；管理员能否在附件管理中看到或处理附件，取决于其能否看到对应的笔记、评论、回复或留言。
- **管理员范围清理**：管理员发起物理附件彻底删除时，只清理该管理员可见的逻辑引用；若仍有不可见引用，物理文件保留，操作对该管理员仍按成功处理，不向其暴露不可见引用的存在。
- **留言**：访客或用户向 1 号管理员提交的站内留言；受托管理员可以像普通用户一样留言，但不接收留言通知，也不能因查看隐藏内容能力读取对其隐藏的留言。

## 规范留言板

- **规范留言板**：前台“留言”页唯一使用的系统留言载体；逻辑所有者和唯一收件人始终是用户 ID 1（`PrimaryAdminUserID`）。
- **留言条目**：规范留言板下的顶层评论。
- **留言回复**：留言条目或其后代下的回复评论，继续继承父线程的可见性约束。
- **留言普通参与者**：普通登录用户或受托管理员；按普通留言可见性查看和互动，不因管理员身份扩大读取范围。
- **留言通知**：`guestbook` 类型站内通知；顶层留言最多创建给 ID 1 的一条通知，ID 1 自己发布留言不创建自我通知。
