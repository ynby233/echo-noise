name: tauri-build-and-release
on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release tag (e.g., V2.3.5)'
        required: true
        type: string
      draft:
        description: 'Create draft release'
        required: false
        type: boolean
        default: true

jobs:
  tauri:
    strategy:
      matrix:
        platform: [macos-latest, windows-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - name: Set app version from input tag
        env:
          TAG_NAME: ${{ inputs.tag_name }}
        run: |
          node -e "const fs=require('fs'),p=require('path');const tag=process.env.TAG_NAME||'';const ver=tag.replace(/^v/i,'').replace(/^V/i,'');const conf=p.join('desktop','tauri','src-tauri','tauri.conf.json');const pkg=p.join('desktop','tauri','package.json');let c=JSON.parse(fs.readFileSync(conf,'utf8'));c.version=ver;fs.writeFileSync(conf,JSON.stringify(c,null,2));let j=JSON.parse(fs.readFileSync(pkg,'utf8'));j.version=tag;fs.writeFileSync(pkg,JSON.stringify(j,null,2));console.log('Version set',{tag,ver});"
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Ensure SQLite file exists
        run: |
          mkdir -p data
          : > data/noise.db
      - name: Build web and copy resources
        run: |
          cd desktop/tauri
          npm ci
          npm run build:web
      - name: Build sidecar
        run: |
          bash desktop/tauri/build-sidecar.sh
      - name: Build Tauri app
        run: |
          cd desktop/tauri
          npm run tauri:build
      - name: Upload artifacts
        uses: softprops/action-gh-release@v2
        with:
          draft: ${{ inputs.draft }}
          tag_name: ${{ inputs.tag_name }}
          files: |
            desktop/tauri/src-tauri/target/**/release/bundle/**/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
