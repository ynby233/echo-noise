# R7：综合回归与交付（快速验收）

日期：2026-09-10。产品代码验收提交：`d09c259c75829c0268152751151e0bf601ab979d`。本轮按用户要求采用快速验收，不把 30–60 分钟连续操作、NAS 部署和 iOS 真机签收伪装成本地已完成项。

## 结论

R1–R6 的本地发布主路径通过，可进入候选镜像和设备签收阶段。本轮没有发现需要继续修改产品代码的新回归；唯一失败是 `mobilebackend` 固定监听 `127.0.0.1:1314`，而该端口已由用户现有的 `admin-ui-proxy.cjs` 占用。按交接约束未停止该进程，也未把此环境冲突归类为产品失败。

## 各工作线状态

| 工作线 | 状态 | 已交付结果与收益 |
| --- | --- | --- |
| R1 | 本地完成 | SQLite 启动、统一一致性备份、重启前暂存恢复、附件归档、失败回退和云同步状态已修复。`cmd/server` 的真实 HTTP ready 集成测试覆盖 20 次空库及 20 次已有库重启，并在本轮全量测试中通过。 |
| R2 | 本地完成 | 通知改为轻量判定、计数选页和最终页 Hydrate；4,000 条单并发列表/未读 p50 分别从 187.1/186.5 ms 降至 45.7/32.2 ms，双并发分别降低 87.5%/88.8%。 |
| R3 | 本地完成 | 后台业务面板迁至异步 section，壳只负责导航与共享编排；修正 `users` 中文显示。本轮前端全量测试中的后台懒加载、草稿、生命周期和权限用例通过。 |
| R4 | 本地完成 | 首页次要功能和媒体运行时按需加载；历史同口径冷读 JS+CSS 从 1211.4 KiB 降至 896.4 KiB（26.0%），请求条目从 57–58 降至 38。后续模块失败恢复与 CSP 兼容修复已纳入当前提交。 |
| R5 | 本地完成 | 编辑器、渲染器、消息列表和 Go 大文件按现有职责拆分；异步结果、监听器及挂载销毁边界已有回归测试。未声称 NAS/iOS 输入性能提升。 |
| R6 | 完成 | `docs/maintenance.md` 已记录入口、模块地图、数据/失败流、联动点、测试命令和平台边界；关键约束保留在责任模块旁。 |
| R7 | 快速本地验收完成 | 全量脚本测试、类型检查、静态生成、Go vet、除端口冲突项外的 Go 全量测试、Linux 双架构编译、四档页面宽度抽查及差异检查完成。 |

## 本轮实际检查

| 检查 | 结果 |
| --- | --- |
| `npm test` | 通过，116 个前端测试文件。 |
| `npx nuxi typecheck` | 通过。 |
| `npm run generate` | 通过，生成 9 个路由；PWA 预缓存 115 项、约 3851.60 KiB。仅有既有 Browserslist 数据过期和 Nuxt CSR 提示。 |
| `go vet ./...` | 通过。 |
| `go test ./...` | 除 `mobilebackend/TestFreshAndroidStandaloneRequiresOwnerSetupBeforeNormalAPIs` 外通过；`internal/services` 113.534 秒通过。失败时 1314 已由用户进程占用，测试等待 15 秒未能启动自己的 endpoint。 |
| `GOOS=linux GOARCH=amd64 go build ./cmd/server` | 通过。 |
| `GOOS=linux GOARCH=arm64 go build ./cmd/server` | 通过。 |
| 页面快速抽查 | 已生成产物和现有带后端代理分别检查；带后端首页在 1440/768/390/320 宽度稳定后均无页面级横向溢出，加载遮罩归零。未执行写入、已读或配置变更。 |
| `git diff --check` | 通过。用户原有未跟踪文件 `docs/direct-update-feasibility-2026-09-10.md` 未改动、未纳入本次提交。 |

静态产物单独预览时热力图 API 返回 HTML，控制台出现 JSON 解析错误；带后端代理健康接口返回 200，故该错误属于无后端的预览方式，不列为产品回归。

## 未实测与发布边界

- 未执行 30–60 分钟 CPU、RSS、goroutine、浏览器 heap/DOM/监听器趋势观察，也未重复 20 次后台面板开关；当前仅能判定快速回归通过，不能声明无泄漏。
- 未登录抽查六个常用后台面板的窄屏保存流程；现有自动化覆盖其加载、草稿、权限和生命周期，但不替代真实账号操作。
- 未在真实 NAS 候选镜像上验证启动、重启及独立目录的备份恢复往返；没有覆盖或修改生产数据。
- 未做 iOS 真机主屏幕安装、键盘、滚动、后台返回、离线和更新提示签收；Chromium 响应式抽查不等于 iOS 验收。
- 未运行 race；本轮 Windows 环境和快速验收不把它作为完成条件。

## 回退

本轮只新增验收记录，无产品行为可回退。若后续候选镜像出现问题，应固定到已验证的旧镜像并停止写入后再处理数据；R1 恢复采用暂存包、启动前应用和失败回退，不能用 `git revert` 代替数据库与附件回退。前端 R3–R5 可按对应提交回退并重新生成，但已安装 PWA 仍需正常完成更新/刷新才能取得回退资源。
